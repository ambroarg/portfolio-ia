// URL de l'API en developpement local (backend lance separement sur 8000).
// L'image Docker remplace ce fichier par config.docker.js, qui laisse la base
// vide pour taper la meme origine a travers nginx.
window.PORTFOLIO_API_BASE = "http://localhost:8000";
